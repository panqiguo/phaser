

# 我的工程风格偏好
- 在场景中挂载单入口, 让我们自己管理游戏状态和实体的生命周期, 而不是依赖引擎的MonoBehaviour
- 尽量把玩法和游戏逻辑放在纯 TS/JS 逻辑层, 保持游戏逻辑和引擎解耦
- UI 以代码驱动为主
- 内容以数据驱动为主, 便于llm阅读和修改游戏数据
- 保持工程结构清晰, 可靠, 可拓展
- 减少混乱的兼容或者对错误的容忍, 对于可能存在的游戏逻辑错误, 或者数据配置错误, 直接assert中断游戏, 打印错误, 最大程度确保游戏的健壮性, 如果有一些建议的fallback, 可以向用户提问

# rules
在代码中UI统一管理, 页面之间保持清晰的组织结构, 适当拆分, 不要贸然把全部UI塞到一个文件里
UI优先使用“约束驱动布局”, 减少像素硬编码布局, 纵向使用流式布局，横向使用分隔split, 根据panel的rect来计算和放置元素的位置
代码内部逻辑/命名保持英文，玩家看到的文案使用中文. 数据的key使用英文, id,名称等值优先使用中文
使用IMGUI 风格来编写和排版 UI，代码顺序就是界面顺序，内容和排版写在一起，改起来像改文档，不要生成一份布局数据再用另一段代码消费它的retained/layout-data 风格

# 测试验证
每次修改代码后必须执行语法检查，防止遗留括号缺失等低级语法错误：进行复杂改动，或者修复排查问题是需要进行最小模拟验证

- 语法检查
```bash
node --check src/文件名.js
```
如果同时改动了多个文件，可批量检查：
```bash
node --check src/gameLogic.js && node --check src/GameScene.js
```

- 最小状态模拟
游戏逻辑与 Phaser 渲染/输入/UI 代码分离，因此可以直接用 Node 运行纯逻辑进行验证。修复 bug 或验证逻辑时，优先使用临时的内联 Node 脚本，不要创建永久测试文件。推荐形式：
```bash
node --input-type=module - <<'JS'
import { ... } from './src/gameLogic.js'

// 构造最小状态、执行操作、断言结果
JS
```

